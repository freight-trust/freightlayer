[
  {
    "description": "standard format: failing",
    "schema": {
      "type": "integer"
    },
    "tests": [
      {
        "description": "integer is invalid (failing test)",
        "data": 1,
        "valid": false
      }
    ]
  }
]
